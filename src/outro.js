var naive = {
  Calc: Calc,
  Canvas: Canvas,
  Game: Game,
  Inputs: Inputs,
  Loader: Loader,
  Loop: Loop,
  Pool: Pool,
  Signal: Signal,
  State: State,
  StateManager: StateManager
};

if (typeof module !== 'undefined') {
  module.exports = naive;
}
