var number = 8;
var color = 'black';

describe('Eight ball', () => {
  it('should be number 8', () => {
    expect(number).toBe(8);
  });
  it('should be black', () => {
    expect(color).toBe('black');
  });
});
